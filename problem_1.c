#include <stdio.h>

int main(){
    int length;
    int breadth = 4;
    printf("enter the length\n");
    scanf("%d", &length);
    printf("the area of this rectangel is %d", length*breadth);
    return 0;
}